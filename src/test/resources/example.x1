package main

sum(v: Int, n: Int): Int {
  for (var i:Int = 0; < i n; i = i + 1) {
    if (i % 2 == 0) {
      v = v + i
    }
  }
  var y:[]Int = new []Int [1,2]
  foreach (x:Int; y) {
    v = v + x
  }
  return v
}